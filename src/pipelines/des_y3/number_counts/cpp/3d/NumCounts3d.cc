// Full (lambda_true, lnM, z) reference number counts — explicit 3d adaptive (formerly `explicit-3d`), C++.
// Physics lives in num_counts_3d_t.hh; see that header for the
// integrand formula, configuration keys, and status notes.
#include "num_counts_3d_t.hh"
#include "utils/module_macros.hh"

DEFINE_COSMOSIS_SCALAR_INTEGRATION_MODULE(NumCounts3d)
