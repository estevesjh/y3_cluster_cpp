"""New maintained DES Y3 pipeline implementations (see README.md).

Import as a package by putting ``<repo>/src/pipelines`` on ``sys.path``;
the CosmoSIS entry-point files under ``observables/`` do this themselves.
"""
