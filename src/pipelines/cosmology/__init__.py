"""Shared halo-model physics for pipeline implementations (see README.md).

Import as a package by putting ``<repo>/src/pipelines`` on ``sys.path``
(the same convention ``des_y3`` and ``shared`` use); the CosmoSIS
entry-point files under here do this themselves.
"""
