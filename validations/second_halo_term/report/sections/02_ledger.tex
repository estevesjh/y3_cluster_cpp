\section{Unit and cosmology ledger}
\label{sec:ledger}

Cross-code comparisons on this chain have historically failed on
conventions, not physics. The harness therefore fixes one explicit
ledger, enforced programmatically: every exchange file
(\texttt{.npz}) carries unit strings, and the comparison script refuses
to compare arrays whose declared conventions differ before conversion.
The factor $10^{12}$ appearing throughout is the area conversion
$1\,\mathrm{Mpc}^2 = 10^{12}\,\mathrm{pc}^2$.

\begin{table}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tabular}{lllll}
\toprule
code & $r$, $R$ & $M$ & $\Sigma$, $\Delta\Sigma$ & $\rho$ convention \\
\midrule
cluster\_toolkit & cMpc$/h$ & $\hMsun$ & $\Msunpc$ comoving &
  $\bar\rho_m = \Omega_m\,\rho_{c,0}$ ($h$-units, no $(1{+}z)^3$) \\
CLensPy & physical Mpc (feed $k h$, $P/h^3$, $R/h$) & --- &
  dimensionless ($\times\,\Omega_m \rho_{c,0} h^2 / h / 10^{12}$) & caller-owned \\
pyccl & Mpc & $M_\odot$ & --- ($\xi$ only) & $\Omega_c = \Omega_m - \Omega_b$ \\
clmm & physical Mpc & $M_\odot$ & $M_\odot/\mathrm{Mpc}^2$ physical &
  $\Omega_{\rm dm} = \Omega_m - \Omega_b$ \\
\bottomrule
\end{tabular}}
\caption{Unit ledger. The clmm comoving bridge is
$\DStwo^{\rm com}(R^{\rm com}) =
\DStwo^{\rm phys}(R^{\rm com}/(1{+}z))/(1{+}z)^2$ (the single-power
variant used in CLensPy's own test disagrees with the three-way
comparison and is rejected).}
\end{table}

Fiducial cosmology (the checked-in dump's
\blk{cosmological\_parameters}): $\Omega_m = 0.311049$, $h = 0.6766$,
$\sigma_8 = 0.8238$, $n_s = 0.9665$, $\Omega_b h^2 = 0.02242$,
$m_\nu = 0$, $w = -1$;
$\rho_{c,0} = 2.77533742639\times10^{11}\,M_\odot\,h^2/\mathrm{Mpc}^3$.
CLensPy is \emph{under test} here, not a gospel reference: its
\pyfile{lensing/profile.py} normalizes the $\Sigma$ and
$\Delta\Sigma$ two-halo paths inconsistently ($\rho_m$ on one, the bare
$10^{12}$ conversion on the other), so the harness uses the raw
\texttt{TwoHaloTerm} transforms with its own conversions
(Section~\ref{sec:remaining} lists all CLensPy findings).
